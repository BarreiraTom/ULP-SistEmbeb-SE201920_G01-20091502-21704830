<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.ulp.se201920_g01_20091502_21704830">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".InserirManutActivity"
            android:label="Inserir Manutenção"></activity>
        <activity
            android:name=".InserirAbastActivity"
            android:label="Inserir Abastecimento"></activity>
        <activity
            android:name=".InserirViagensActivity"
            android:label="Inserir Viagem"></activity>
        <activity
            android:name=".InserirSegurosActivity"
            android:label="Inserir Seguro"></activity>

        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>